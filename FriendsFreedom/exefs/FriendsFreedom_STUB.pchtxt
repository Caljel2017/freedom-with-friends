@nsobid-REPLACE_WITH_YOUR_BUILD_ID
# Friends Freedom — ACNH multiplayer creative / terraform unlocks
# Game version: UNKNOWN — run scripts/New-PatchStub.ps1 with your build ID
#
# STATUS: STUB ONLY
# This file is intentionally inert (no @enabled patches).
# Enabling empty stubs is safe; adding fake offsets is not.

@flag print_values
@flag offset_shift 0x100

# Add real patches here after reversing your dump, for example:
# @enabled Visitor terraform allow
# <offset> mov w0, #1
# <offset> ret
